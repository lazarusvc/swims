@model SWIMS.Models.SW_organization

@{
    ViewData["title"] = "New village council";
    ViewData["subTitle"] = "Add a new local organisation.";
    ViewData["description"] = "Create a village council or local organisation entry that can be linked to beneficiaries, programmes and cases in SWIMS.";
}

<div class="row">
    <div class="col-xl-7 col-lg-8">
        <div class="card radius-12">
            <div class="card-header d-flex align-items-center justify-content-between gap-2 flex-wrap">
                <h5 class="card-title mb-0">Create village council</h5>

                <a asp-action="Index"
                   class="btn btn-sm btn-outline-secondary d-flex align-items-center gap-1">
                    <iconify-icon icon="lucide:arrow-left" class="fs-14"></iconify-icon>
                    <span>Back to list</span>
                </a>
            </div>

            <div class="card-body">
                <form asp-action="Create" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                    <div class="mb-3 row">
                        <label asp-for="vendor_id" class="col-sm-3 col-form-label"></label>
                        <div class="col-sm-9">
                            <input asp-for="vendor_id" class="form-control" />
                            <span asp-validation-for="vendor_id" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="mb-3 row">
                        <label asp-for="name" class="col-sm-3 col-form-label"></label>
                        <div class="col-sm-9">
                            <input asp-for="name" class="form-control" />
                            <span asp-validation-for="name" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="mb-3 row">
                        <label asp-for="type" class="col-sm-3 col-form-label"></label>
                        <div class="col-sm-9">
                            <input asp-for="type" class="form-control" />
                            <span asp-validation-for="type" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="mb-3 row">
                        <label asp-for="active" class="col-sm-3 col-form-label"></label>
                        <div class="col-sm-9">
                            <input asp-for="active" class="form-control" />
                            <span asp-validation-for="active" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="d-flex justify-content-end gap-2 mt-3">
                        <a asp-action="Index" class="btn btn-outline-secondary">
                            Cancel
                        </a>
                        <button type="submit" class="btn btn-primary">
                            Create village council
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
